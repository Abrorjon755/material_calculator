class Constants {
  const Constants._();

  // Settings
  static const String theme = "settings.theme";
  static const String locale = "settings.locale";

  // Storage
  static const String history = "storage.history";
}
